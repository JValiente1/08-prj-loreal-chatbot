// Example local config file.
// This is optional because the app uses /api/chat by default.
// Only add CHAT_API_URL here if you want to point to a different API URL.

const CHAT_API_URL = "/api/chat";
